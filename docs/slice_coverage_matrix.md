# Slice Coverage Matrix

Every checked fixture generates a temporary reduced workspace and runs
`cargo check` against the generated slice. The tests also assert that expected
dead functions, items, modules, tests, and local crates are absent.

## Verified Slices

| Area | Verified by | Coverage |
| --- | --- | --- |
| Cross-crate free functions | `opensource_core` unit fixture | Root function, direct calls, transitive calls across five crates |
| Private helpers | `data_items.rs`, root fixture | Private functions reached from exported code are retained |
| Public dead APIs | all integration fixtures | Public functions not reached from the root are pruned |
| Structs | `data_items.rs`, `uniffi_mobile.rs`, `component_matrix.rs` | Unit structs, tuple structs, named-field structs, generic structs |
| Enums | `data_items.rs`, `trait_ufcs.rs`, `uniffi_mobile.rs`, `component_matrix.rs` | Unit, tuple, and struct-like variants used in signatures, bodies, and patterns |
| Unions | `component_matrix.rs` | Union item reached through a retained struct field and constructor literal |
| Type aliases | `data_items.rs`, root fixture, `component_matrix.rs` | Aliases used in signatures, fields, and local bindings |
| Consts/statics | root fixture, `data_items.rs`, `component_matrix.rs`, `pattern_constants.rs`, `manifest_hardening.rs`, `rule_database.rs` | Constants/statics used in bodies, fields, associated const values, unqualified match patterns, const-to-const arithmetic, const array lengths, static initializer closures, and implicit `format!("{NAME}")` captures |
| Inherent impl methods | all integration fixtures | Associated constructors, receiver calls, async methods, generic impl blocks |
| Trait definitions | root fixture, `trait_ufcs.rs`, `uniffi_mobile.rs`, `component_matrix.rs`, `fast_macro_use.rs`, `rule_database.rs` | Trait items retained when trait impl methods are reachable, when a retained type exposes a public local trait impl surface, when a receiver call resolves to a trait default method, and when the trait itself is a selected item root; traits retained only as object/type surfaces keep required or referenced members and prune optional default-method dependencies |
| Trait impl methods | root fixture, `trait_ufcs.rs`, `uniffi_mobile.rs`, `component_matrix.rs`, `manifest_hardening.rs`, `rule_database.rs` | Receiver calls, explicit `<Type as Trait>::method`, `Trait::method(&receiver, ...)`, generic trait-bound receiver calls, trait-bound method return chaining, format-only `Display`, and `to_string()`-required `Display` impls; trait impls rendered for surface compatibility keep required/reachable members while pruning dead optional overrides |
| Ambiguous method fallback | core production-readiness tests, Litter pinned corpus | Unresolved receiver-less method calls retain only a single unambiguous same-name local method; ambiguous same-name sets are capped and reported as feedback hazards instead of pulling unrelated subsystems into the slice |
| Trait impl peers | `component_matrix.rs` | Required peer methods and associated type/const items are retained so trait impls compile |
| Dynamic dispatch boundaries | core production-readiness tests, `fast_macro_use.rs` | Retained direct callback inputs such as `fn(...)` parameters and borrowed `&dyn Trait` parameters are preserved as selected API boundaries and downgraded to feedback-dischargeable warnings; retained owned/returned/stored `dyn Trait` and local function-pointer type surfaces remain production-blocking until semantic analysis can prove concrete dispatch/callback edges, and report package/module/file/line plus the retained surface text |
| Associated types/consts | `component_matrix.rs` | Associated type and associated const dependencies are followed from retained impls |
| External trait imports | `manifest_hardening.rs` | Extension traits such as `tokio::io::AsyncReadExt`, private std traits such as `std::io::Write`, and trait-method imports without an `Ext` suffix such as `base64::Engine` are retained; derive-only imports such as `serde::Serialize` are pruned per rendered module instead of being kept because another module in the package uses the same derive |
| External modules | `module_reexports.rs`, `component_matrix.rs`, core output-safety tests | `mod file;`, `mod/name/mod.rs`, nested modules, empty dead module pruning, and rejection of path-attributed modules that would escape package output |
| Inline modules | `module_reexports.rs`, `component_matrix.rs` | Inline modules with retained reexports are preserved |
| Reexports | `module_reexports.rs`, `component_matrix.rs`, `manifest_hardening.rs`, `fast_macro_use.rs` | Reachable `pub use` targets are kept; renamed dependency type reexports used through local barrels keep their concrete dependency target item; multi-hop local and dependency barrel reexports resolve live function aliases through support packages while pruning dead sibling helpers; dead grouped and external `pub use` reexports are pruned; public local glob prefixes inside inline modules are dropped when the inline child module is not rendered |
| Dependency aliases | root fixture, `module_reexports.rs`, `manifest_hardening.rs` | `package = "..."` dependency aliases and renamed imports; renamed local imports are pruned when their resolved target is removed, even when a retained local binding has the same visible name; local path dependencies are not retained only because an ordinary local identifier matches the dependency alias |
| Rule database | `rule_database.rs`, `rule_catalog.rs`, `docs/rule_database.md`, `docs/rule_catalog.md`, `scripts/fast_rule_catalog_loop.sh` | Data-oriented seed corpus for generic rule growth: 132 focused cargo-checked reducer/render/manifest rules plus a generated 1,200-row catalog that now executes every row as a real batched `opensource_core::generate()` slice with enforceable coverage-family mapping spanning import, macro, trait, dyn, include, build, UniFFI, manifest, repair, and cfg combinations without project-specific names. The generated catalog uses token, asset, hazard, and `syn` syntax assertions for speed; cargo compilation remains in the focused fixtures and real-repo probes. Executable coverage includes grouped and chained public reexport pruning, removed public alias pruning despite local binding shadows, module-scoped import liveness, renamed local type/trait imports used only by retained surfaces, renamed dependency type and multi-hop function reexports through local/dependency barrels, facade const/function alias pruning, owned/stored/returned dynamic dispatch hazards, retained direct callback/FFI callback API boundary warnings, inline and aliased callback/future dynamic hazards, nested callback stores, bare dyn aliases inside `OnceLock<Arc<_>>`, facade-reexported boxed I/O dyn aliases, child-module `super::{...}` parent import liveness, private-child wildcard import/reexport pruning, nested private facade chain pruning, local facade glob pruning, dependency-crate alias manifest edges, auto-trait object casts, pruned private dynamic fields, private generic field pruning when type params remain used, local aliases named like prelude/std types, result aliases, alias chains, nested `Result<Option<_>, _>` aliases, returned `Arc` handle records, nested `Option<Vec<Dto>>` and map DTO surfaces, mirror `TryFrom` request conversions, fallible `Option<Vec<T>>`/`transpose()?` conversions, generic `TryInto<Target>` bridge conversion pruning, precise `?` error conversion retention, wire-error fallback conversion retention, dead inherent impl method pruning, tuple newtype surfaces, UniFFI-style callback facades, UniFFI enum struct-variant payload surfaces, returned subscription-object impl surfaces, split exported wrapper impl pruning, `impl Iterator<Item = Dto>` returns, enum variant constructors used as functions, struct update defaults, method references in iterator adapters, boxed future return aliases, const-generic array surface lengths, pruned type-only trait method hazards, required/reachable trait surface member pruning, receiver/type-input precise trait peer retention, `async_trait` trait-object boundaries, retained `include_bytes!` and `include_str!` asset copying, selected section `include_str!` asset copying, concat-array `include_str!` asset copying, `OnceLock` initializer `include_str!` asset copying, function-local static `include_bytes!` copying, source `include!` blockers including fallback-retained inline modules, build-script `rustc-env`/`env!` and `option_env!` blockers, retained macro-body env/option_env hazard scanning, `OUT_DIR` source include blockers, nested callback/future aliases, `Option<Arc<dyn Trait>>` callback slots, `pub(crate)` macro helper reexports, qualified `serde_json::json!`, serde helper-attribute string paths, unqualified proc-macro attribute import retention, `with = "module"` helpers, flatten contract fields, nested flatten payload DTOs, `deserialize_with`, alias fields, transparent records, `skip_serializing_if`, tagged/default/untagged enum contracts, nested tagged patch-path contracts, nested discriminated response envelopes, multiple serde deserialize helpers, method-string typed dispatch with unknown fallback, dependency aliases used only inside macro bodies, macro-generated live items including zero-argument item macro invocations, fixed-name generated functions, inline-module item macro invocations, shadowed macro definitions resolved by lexical scope, macro expression argument receiver calls, macro metavariable method and enum-variant dependencies, FFI extern function/static declaration pruning, source path-dependency trait imports by trait method names, derive impl closure through array/slice/pointer field types, associated-type equality method chains, tuple/struct/typed-parameter destructuring receiver inference, for-loop item inference, free-function closure argument typing, `Option`/`Result` closure combinator payload/error typing, project-local generic closure methods with receiver generic substitution, rendered trait impl surface dependencies, support path bundle and nonstandard-lib rewriting/pruning, local build-dependency pruning without retained build scripts, local dependency edge pruning even when the target package is retained elsewhere, local dependency alias false-positive pruning, let-else slice patterns, static initializer closures including `OnceLock::get_or_init`, runtime singleton facades, format-captured consts, const chains, default trait methods with impl associated consts, associated projection impls, precise `TryFrom` conversion retention, bidirectional `From` roundtrip retention, private dead enum variant pruning, cfg-gated extern import preservation, unsafe no_mangle FFI exports, JNI extern system roots, clap command derives, thiserror field contracts, async I/O poll impls, runtime singleton registries, platform cfg asset bundles, and serde custom numeric helpers |
| Real Litter probes | `docs/litter_probe_report.md` | Five temp-copy `codex-ipc` roots accepted production validation with compiler feedback: compact protocol/pending/transport roots generated 12-15 files, while bridge/state roots exposed a remaining support-package size problem at 850+ files |
| Fast macro/use corpus | `fast_macro_use.rs`, `fixtures/fast_macro_use`, `scripts/fast_fixture_loop.sh` | Tiny checked-in multi-package workspace for quick iteration across Litter-like renamed imports, local shadows, custom derives, serde-style helper attributes including custom `_serde` helper paths, error-like derives, UniFFI-shaped record/enum/object/export/constructor/setup surfaces, async-runtime exported impls and async exported roots, retained callback/function-pointer signatures, callback-provider setter flows that convert `Box<dyn Trait>` into stored `Arc<dyn Trait>`, private FFI barrels with subscription objects and dead sibling exports, request-helper macro facades that build enum variants and call request helpers, platform `cfg_attr` modules, `cfg(any(...), not(...))` module pairs, `From`/`TryFrom` conversion impls, dependency type barrels with live/dead renamed reexports, macro-declared items/functions, macro_rules with `pub(crate) use` reexports, include-generated source, static `include_str!` assets, private inline facade reexports, prelude/module reexports, inline child imports, generic struct/trait/impl headers with `where` bounds, associated type/const trait impls, `FromStr`/`Display`/`Deref` std trait edges, UFCS trait calls, const generics, type aliases, const/static items, const match patterns, struct-like enum variants, associated trait items, trait imports, dead owned-dyn registries, and dead pruning across one-root/two-root/three-root/item-root slice angles |
| Workspace member globs | `component_matrix.rs` | `[workspace].members = ["crates/*"]` expansion |
| Single-package binary roots | `manifest_hardening.rs`, `docs/real_rtk_slice_report.md` | Non-workspace crates, `src/main.rs` parsing, and stub binary roots |
| Macro-declared modules | `manifest_hardening.rs`, `docs/real_rtk_slice_report.md` | `automod::dir!(... "path")` directories are parsed and rendered as explicit reduced `mod` declarations |
| External dependencies | `uniffi_mobile.rs`, `component_matrix.rs` | Used workspace dependencies such as `serde = { features = ["derive"] }` are preserved; unused external deps with no retained source reference are pruned |
| Build dependencies | `manifest_hardening.rs`, `docs/real_rtk_slice_report.md` | `[build-dependencies]` are retained when `build.rs` is copied and pruned from packages whose generated manifest has no retained build script |
| Target dependencies | `manifest_hardening.rs` | `[target.'cfg(...)'.dependencies]` tables are preserved when retained source references them |
| Required target features | `manifest_hardening.rs`, CLI unit tests | `required-features` on retained example/test/bench/bin targets are reported and validation rejects Cargo argument sets that would skip the selected target or leave required bin features inactive |
| Feature pruning | `manifest_hardening.rs` | Optional dependency feature entries are removed when the dependency is pruned |
| Local path dependency pruning | `uniffi_mobile.rs`, `component_matrix.rs`, `manifest_hardening.rs` | Unused local crates, source imports, dependency edges, and optional feature entries are omitted from manifests even when the target package is retained by another live edge |
| Non-workspace path dependencies | `manifest_hardening.rs`, core production-readiness tests, Litter pinned corpus | Retained source references to direct or target-specific path dependencies outside the workspace are copied into generated-local `support/` packages when the path resolves to a local Cargo package root; no-build support packages copy only the library external-module graph plus static include assets, skip `#[cfg(test)]` modules and orphan Rust files, drop dev-dependencies, build-dependencies, `src/bin`, and test/example/bench target surfaces, avoid copying dev/build-only support path packages, and can prune dead sibling items, dead local-self impls, child-module items, public facade/transitive-facade/local-glob reexport leaves, explicit dependency-crate reexport aliases, private local-glob imports, and dead local path dependencies when retained source gives concrete dependency public names; copied support packages are scanned after rendering for build scripts, source `include!`, `OUT_DIR`, unresolved/absolute/external file includes, and compile-time env hazards; opaque support module graphs, external dependency glob reexports/imports, unresolved relative paths, and ambiguous unsupported glob prefixes fall back conservatively, and uncopyable path entries remain production-blocking with manifest/package/table details |
| Macro definitions | `component_matrix.rs` | Used `macro_rules!` definitions are kept, unused macro definitions are pruned, and direct helper calls inside retained macro bodies are followed |
| Item macro-generated items | `manifest_hardening.rs`, `docs/real_rtk_slice_report.md` | Item macro invocations such as `lazy_static!` are retained when generated identifiers are referenced by reachable code; inline modules that retain macro-generated source remain valid import targets for child modules |
| Derive and helper-attribute dependencies | `manifest_hardening.rs`, `fast_macro_use.rs` | Retained derive/custom-attribute surfaces keep only the referenced exported proc-macro derive/attribute/function-like entries from local proc-macro crates, prune unused exported proc macros and private helpers, keep unqualified retained custom-attribute imports in scope, promote helper functions/items referenced by retained helper attributes into the graph before rendering, preserve path-qualified derive/attribute macros without retaining unused simple imports, copy live proc-macro helper library crates as pruned `support/` packages instead of root workspace members, omit dead proc-macro helper crates, and close macro-bearing inherent impl surfaces over dependencies even inside inline private facade modules |
| Build-script assets | `manifest_hardening.rs`, `docs/real_rtk_slice_report.md`, core production-readiness tests | Non-Rust assets referenced by `build.rs` string literal paths are copied without copying unrelated package docs/config; retained build scripts are production-blocking with package/path details until hermetic build-script modeling exists |
| Source include assets | `manifest_hardening.rs`, `fast_macro_use.rs`, `rule_database.rs`, core production-readiness tests | Files referenced by retained `include!`, `include_str!`, and `include_bytes!` static literal/`concat!`/`CARGO_MANIFEST_DIR` paths are copied next to the sliced source; retained `include!` Rust source, unknown file include paths, absolute paths, external paths, and `OUT_DIR` generated assets are production-blocking with structured package/module/file/line details, including `OUT_DIR` includes nested in retained inline modules |
| FFI extern blocks | `rule_database.rs` | Foreign `extern "C"` declarations called by retained roots are kept by symbol name, while dead sibling declarations in the same foreign block are pruned so FFI surfaces do not over-retain unrelated native calls |
| Compile-time environment macros | `rule_database.rs`, core production-readiness tests | Retained `env!`/`option_env!` macros that read outside Cargo manifest-derived package metadata are production-blocking because they can embed machine-local state, and report structured package/module/file/line details |
| Symlink copy boundaries | `manifest_hardening.rs` | Internal package symlinked include assets are copied at the source-visible link path, while symlinked source or build assets resolving outside the package root are not copied |
| Workspace patches and locks | `manifest_hardening.rs`, CLI unit tests, core production-readiness tests | Root `[patch.*]` tables and `Cargo.lock` are preserved; production validation adds `--locked` for source workspaces with lockfiles so sliced workspaces keep the source repository's dependency resolution, and uncopyable patch/replace path entries are production-blocking with manifest and subject details until copied or pruned |
| Workspace profiles | `manifest_hardening.rs` | Root `[profile.*]` tables are preserved so generated validation does not fall back to Cargo profile defaults |
| Toolchain context | `manifest_hardening.rs` | Root `rust-toolchain.toml` / `rust-toolchain` files are copied so generated validation uses the source workspace's pinned Rust toolchain |
| Analyzer modes | analyzer unit tests, CLI option parsing, Litter pinned corpus | The default CLI feature set enables `ra-hir` and defaults to bounded rust-analyzer HIR semantics for local workspace crates; `--production` defaults to `ra-hir-proc-macros`, but the bounded default skips proc-macro/build-script artifact loading while dependency artifacts remain excluded, avoiding a known incompatible rust-analyzer load path; full Cargo dependency artifact discovery and sysroot proc-macro loading are opt-in with `OPENSOURCE_RA_PROC_MACRO_LOAD_DEPS=1` because they timed out on pinned Litter; `--analyzer ra-feedback` is the copy/prove/cut proof-of-concept that queries rust-analyzer outgoing call hierarchy for selected-root and syntactic-retained owner files before applying the same additive retained-edge map; semantic walking remains bounded to workspace Rust files and reports when proc-macro expansion is unavailable; files containing selected roots are analyzed first, exact project-local RA method/path targets are mapped into generic reduction hints and applied additively, per-file semantic inventory lets the production gate scope semantic warning hazards to retained slice files before selected-root/workspace fallbacks, while `syn` remains an explicit fallback |
| Final production gate | CLI production gate tests | `--production` records `production_ready=accepted` only when compiler feedback passes and no production hazards remain; warning-only hazards now record `production_ready=review_required` instead of being mislabeled as production-ready |
| Feedback runner scale | core feedback tests, Litter pinned corpus | Cargo stdout/stderr are drained while `cargo check --message-format=json` runs, so large dependency graphs cannot deadlock the feedback loop by filling captured output pipes; plain feedback, repair, and production-matrix loops stop on exact repeated diagnostics or repeated diagnostic shapes; repair mode removes repairable unused imports before accepting warning-bearing checks |
| Async functions | `component_matrix.rs` | Async root and async impl method slices build |
| Unit tests | all generated fixtures | `#[test]` functions and `#[cfg(test)]` modules are dropped |
| Non-test cfg roots and surfaces | core production-readiness tests, CLI production gate tests | Selected roots and retained source surfaces behind feature/platform cfgs, including `runtime-benchmarks`, are retained in the graph instead of being pruned as tests; selected cfg-gated roots remain production-blocking unless the cfg expression is proven covered by validation Cargo feature args and host/explicit-target `rustc --print cfg` output, including recognized `all(...)`, `any(...)`, and `not(...)` combinations, and non-root feature cfg surfaces carry structured package/module/file/cfg details that drive an extra production source/generated feature-matrix check |
| UniFFI-shaped API | `uniffi_mobile.rs`, `uniffi_setup.rs`, `fast_macro_use.rs` | FFI-facing records/enums, inactive `cfg_attr(..., uniffi::...)`, retained `uniffi::setup_scaffolding!()`, serde DTOs, mobile bridge shape, Arc-returning object constructors, macro-exported inherent impl surfaces, callback interface traits, stored callback registries, and callback/future aliases |
| Real UniFFI project | `docs/real_litter_uniffi_slice_report.md` | Litter `codex-mobile-client` cloud sync and preferences slices build after pruning |
| Real high-star Rust project | `docs/real_rtk_slice_report.md` | RTK `find_corrections` and `filter_json_string` slices build after binary, automod, macro, and build-script hardening |

## Current Boundaries

These are tracked limitations, not silently claimed support:

| Area | Boundary |
| --- | --- |
| Full rustc name resolution | The default CLI flow applies exact local rust-analyzer method/path edges, but the reducer still uses syntactic fallback logic and does not yet replace rustc or rust-analyzer name resolution for trait impl lookup, macro-expanded items, generated source, dynamic dispatch, or all cfg-active reachability decisions |
| Macro-expanded dependencies | The production analyzer can request rust-analyzer proc-macro expansion, but macro-expanded items are not yet mapped into first-class retained source; retained custom derives, custom attributes, module-boundary custom attributes, and non-builtin macro invocations are preserved verbatim and reported with the exact macro path in production hazard details, retained helper-attribute paths are promoted into the graph, local proc-macro crates are retained at referenced-export granularity with live helper crates copied through support packages, and compiler feedback remains required until macro-expanded inventory feeds the reachability graph |
| Generic trait receiver inference | Local trait-bound receiver calls such as `value.trait_method()` are followed for named type parameters, `where` bounds, `impl Trait` parameters, explicit local bindings, and simple transparent wrappers; full rustc-equivalent inference for associated types, substitutions through arbitrary containers, and complex projection bounds still requires the semantic oracle |
| Function pointers and dynamic dispatch | Function pointer calls, trait-object calls, and callback registries are not followed; selected direct API boundary inputs such as `fn(...)` parameters and borrowed `&dyn Trait` parameters are preserved as review-required warnings, while owned, returned, stored, aliased, registry, and local function-pointer surfaces remain production-blocking and report structured package/module/file/line details |
| Build scripts and `include!` source | `build.rs` and referenced non-Rust assets are copied; generated Rust files under `OUT_DIR`, build-script side effects, and any retained `include!` Rust source are not semantically modeled, so they are production-blocking hazards |
| Compile-time environment | `env!`/`option_env!` values outside Cargo package metadata are not modeled and are production-blocking |
| Feature/platform cfg matrices | Only `#[cfg(test)]` is pruned as test-only; broader feature/platform matrix evaluation is conservative, cfg-gated selected roots are production-blocking unless concrete Cargo feature cfgs are covered by `--features`/`--all-features` or recognized target predicates are proven by host/explicit-target `rustc --print cfg`; compound `all(...)`/`any(...)`/`not(...)` expressions are evaluated with tri-state fail-closed semantics, retained non-root Cargo feature cfg surfaces are validated by a bounded production matrix pass, and custom cfgs still require the semantic oracle |
| External crate pruning | External dependencies are pruned when their crate alias is absent from retained source tokens; full rustc-level static unused-import analysis is not implemented, so repair/production validation relies on compiler diagnostics for warning cleanup |
| Non-workspace path dependencies | Copying is bounded to local path package roots that expose `Cargo.toml`; uncopyable path entries remain production-blocking with structured manifest details |

## Verification Commands

```sh
cargo test --workspace
cargo run -p opensource_cli --bin slicers -- --check . /tmp/slicers-proof
cargo check --manifest-path /tmp/slicers-proof/Cargo.toml
```
