use opensourced::opensourced;

#[opensourced]
pub fn selected_for_btreeset_ref_payload_report(raw: &str) -> String {
    for_btreeset_ref_payload_api::selected_for_btreeset_ref_payload_report(raw)
}

pub fn dead_for_btreeset_ref_payload_report(raw: &str) -> String {
    for_btreeset_ref_payload_api::dead_for_btreeset_ref_payload_report(raw)
}
