use opensourced::opensourced;

#[opensourced]
pub fn selected_iterator_flatten_option_array_report(raw: &str) -> String {
    iterator_flatten_option_array_api::selected_iterator_flatten_option_array_report(raw)
}

pub fn dead_iterator_flatten_option_array_report(raw: &str) -> String {
    iterator_flatten_option_array_api::dead_iterator_flatten_option_array_report(raw)
}
