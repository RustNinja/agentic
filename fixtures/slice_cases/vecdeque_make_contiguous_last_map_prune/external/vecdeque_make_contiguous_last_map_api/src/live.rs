pub fn selected_vecdeque_make_contiguous_last_map_report(raw: &str) -> String {
    vecdeque_make_contiguous_last_map_model::selected_vecdeque_make_contiguous_last_map(raw)
}

pub fn dead_live_vecdeque_make_contiguous_last_map_report(raw: &str) -> String {
    format!("dead-live-vecdeque-make-contiguous-last-map-report:{raw}")
}
