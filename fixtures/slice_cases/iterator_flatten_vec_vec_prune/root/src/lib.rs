use opensourced::opensourced;

#[opensourced]
pub fn selected_iterator_flatten_vec_vec_report(raw: &str) -> String {
    iterator_flatten_vec_vec_api::selected_iterator_flatten_vec_vec_report(raw)
}

pub fn dead_iterator_flatten_vec_vec_report(raw: &str) -> String {
    iterator_flatten_vec_vec_api::dead_iterator_flatten_vec_vec_report(raw)
}
