use opensourced::opensourced;

#[opensourced]
pub fn selected_vecdeque_make_contiguous_sort_report(raw: &str) -> String {
    vecdeque_make_contiguous_sort_api::selected_vecdeque_make_contiguous_sort_report(raw)
}

pub fn dead_vecdeque_make_contiguous_sort_report(raw: &str) -> String {
    vecdeque_make_contiguous_sort_api::dead_vecdeque_make_contiguous_sort_report(raw)
}
