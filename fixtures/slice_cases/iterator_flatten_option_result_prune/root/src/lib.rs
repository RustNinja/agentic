use opensourced::opensourced;

#[opensourced]
pub fn selected_iterator_flatten_option_result_report(raw: &str) -> String {
    iterator_flatten_option_result_api::selected_iterator_flatten_option_result_report(raw)
}

pub fn dead_iterator_flatten_option_result_report(raw: &str) -> String {
    iterator_flatten_option_result_api::dead_iterator_flatten_option_result_report(raw)
}
