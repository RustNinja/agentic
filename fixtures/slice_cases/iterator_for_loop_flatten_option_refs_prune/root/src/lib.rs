use opensourced::opensourced;

#[opensourced]
pub fn selected_iterator_for_loop_flatten_option_refs_report(raw: &str) -> String {
    iterator_for_loop_flatten_option_refs_api::selected_iterator_for_loop_flatten_option_refs_report(
        raw,
    )
}

pub fn dead_iterator_for_loop_flatten_option_refs_report(raw: &str) -> String {
    iterator_for_loop_flatten_option_refs_api::dead_iterator_for_loop_flatten_option_refs_report(
        raw,
    )
}
